[phases.setup]
aptPkgs = ["openssl", "libssl3"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npm run build", "mkdir -p db", "npx prisma db push --skip-generate"]

[start]
cmd = "node .next/standalone/server.js"
