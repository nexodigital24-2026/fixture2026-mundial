import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mundial 2026 — Simulador Digital",
  description: "Simulador interactivo del FIFA World Cup 2026. Cuenta regresiva, fase de grupos, llaves eliminatorias y sedes.",
  keywords: ["Mundial 2026", "FIFA World Cup", "Simulador", "Fixture", "Eliminatorias"],
  authors: [{ name: "24 Horicias" }],
  icons: {
    icon: "/logo-24.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
